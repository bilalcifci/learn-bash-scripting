#!/bin/bash

# Author: Bilal Cifci
# Date Created: 30/7/2025
# Last Modified: 30/7/2025

# Description:
# This script creates a compressed archive (.tar file) of all files in the user's home directory.
# The backup archive is saved in the '~/Desktop/GitCloneRepo/learn-bash-scripting/section_1/my_backup/' directory.
# The archive file is named with a timestamp (e.g., backup_30-07-2025_17-45-30.tar).
#
# Açıklama:
# Bu betik, kullanıcının ev dizinindeki tüm dosyaları içeren bir arşiv (.tar) dosyası oluşturur.
# Oluşturulan yedek dosyası, '~/Desktop/GitCloneRepo/learn-bash-scripting/section_1/my_backup/' dizinine kaydedilir.
# Dosya adı tarih-saat bilgisi içerir (örn: backup_30-07-2025_17-45-30.tar).

# Define backup directory
BACKUP_DIR="$HOME/Desktop/GitCloneRepo/learn-bash-scripting/section_1/my_backup"

# Create the backup directory if it doesn't exist
mkdir -p "$BACKUP_DIR"

# Create tar archive with timestamp in name
tar -cvf "$BACKUP_DIR/backup_$(date +%d-%m-%Y_%H-%M-%S).tar" "$HOME"/* 2>/dev/null

# Exit script
exit 0

